<%= form_with(model: host_room, local: true) do |form| %>
  <% if host_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(host_room.errors.count, "error") %> prohibited this host_room from being saved:</h2>

      <ul>
      <% host_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
