<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head'%>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= render 'host/header' %>
      <%= yield %>
    <%= render 'host/footer' %>
  </body>
</html>
